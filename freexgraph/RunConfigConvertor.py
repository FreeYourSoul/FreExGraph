from freexgraph.FreExGraph import FreExGraph, FreExNode


def from_runconfig_to_feg(pipe_parsed_rc: dict) -> FreExGraph:
    """ Retrieve an execution graph from a pipe-parsed runconfig dictionary
    :param pipe_parsed_rc: pipe parsed dictionary of a runconfig file to convert to execution graph
    """
    result = FreExGraph()

    # 

    return result
